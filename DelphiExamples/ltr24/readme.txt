ƒанный пример демонстрирует работу с модулем LTR24 из среды Delphi.
ѕример содержит файл проекта дл€ среды "Delphi 7" (ltr24_delphi.dpr) и дл€
среды "Embarcadero Delphi XE2" (ltr24_delphi.dproj).

¬ проекте необходимо указать путь к файлам ltrapi.pas, ltrapidefine.pas,
ltrapitypes.pas, ltr24api.pas новой версии, которые устанавливаютс€ вместе
с библиотеками ltr и необходимы дл€ сборки данного примера
(LTR_INSTALL_DIR/include/pascal2).

ѕуть можно задать следующим образом:
    Delphi 7    - "Project->Options->Directories/Conditionals->Search path",
    Delphi XE2  - "Project->Options->Delphi Compiler->Search path"

ѕроект демонстрирует в первую очередь как можно работать с модулем LTR24,
а не как нужно программировать в среде Delphi.

ѕример демонстрирует реализацию сбора дл€ заданных настроек.
ѕрием данных выполн€етс€ в отдельном потоке, реализованном в классе TLTR24_ProcessThread.
ѕрием осуществл€етс€ блоками, размер блока определ€етс€ количеством отсчетов, которые
передает модуль за заданное врем€ (0.5 с)
ѕо приему блока  выводитс€ среднее значение по каждому разрешенному каналу.
